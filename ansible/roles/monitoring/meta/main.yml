---
galaxy_info:
  role_name: monitoring
  author: devops-student
  description: Deploy Loki + Promtail + Grafana logging stack via Docker Compose
  min_ansible_version: "2.16"

dependencies:
  - role: docker
